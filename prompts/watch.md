---
description: Use the default media transcription and video-analysis skill on a URL or local file
argument-hint: "<video-url-or-path> [question]"
---

Use the `watch-video` skill with these arguments:

$ARGUMENTS

First produce the transcript/frames bundle, then answer the user's question from that evidence.

If no arguments were provided, ask the user for a video URL or local video path and what they want to know about it.
